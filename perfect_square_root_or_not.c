#include<stdio.h>
#include<math.h>
int main()
{
    int n,sq;
    scanf("%d",&n);
    sq=sqrt(n);
    float res;
    res=sqrt(n);
    if(res/sq==1)
    printf("True");
    else
    printf("False");
    return 0;
    
}